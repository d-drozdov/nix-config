# Darwin Config

```
.
├── casks.nix           # Homebrew casks go here
├── default.nix         # Set up system specific things, install packages
├── home-manager.nix    # Manage homebrew and config files
└── packages.nix        # Manage Nix Packages
```